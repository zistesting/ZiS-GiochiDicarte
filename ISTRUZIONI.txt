PARTE 1 di 3 - tutto il progetto tranne le immagini  (63 file)

ORDINE: carica PRIMA la Parte 3 (mazzo bergamasco), POI questa.
Il codice qui dentro nomina le immagini berg_* una per una: se arriva per primo, la build
fra i due caricamenti fallisce con "resource berg_0_1 not found". Nessun danno, ma ti
arriva una mail di errore da GitHub per niente.

La Parte 2 (le altre immagini) e' invariata: non va ricaricata.

--- caricamento ---

NON SERVE CANCELLARE NIENTE, tranne UNA cosa (vedi sotto): i nomi sono gli stessi che
ci sono gia' nel repository, quindi caricandoli si sovrascrivono.

Vai nella pagina principale del repository, poi Add file -> Upload files, e trascina
dentro il CONTENUTO di questa cartella (non la cartella: quello che c'e' dentro).
Le sottocartelle vengono create da sole.

NON caricare questo file ISTRUZIONI.txt: serve solo a te.

Attenzione a due elementi che iniziano con il punto e che alcuni sistemi nascondono:
  .github/workflows/build-apk.yml
  .gitignore
Su Windows: Esplora file -> Visualizza -> spunta "Elementi nascosti".
Su macOS: Cmd+Shift+punto nel Finder.

--- >>> UN FILE VA CANCELLATO A MANO <<< ---

  app/src/main/res/raw/keep.xml

Caricare dei file non ne cancella altri, quindi questo va tolto dal repository a mano:
apri il file su GitHub, menu ... in alto a destra, Delete file, Commit.
Se la cartella res/raw resta vuota GitHub la fa sparire da sola.

Non e' obbligatorio: lasciandolo l'app compila lo stesso, e' solo un file che non serve
piu' a nessuno.

--- cosa e' cambiato ---

TERZO MAZZO: "Bergamasche", accanto a ZiS e Tradizionale. Si sceglie in Impostazioni,
sezione Mazzo. Le immagini sono nella Parte 3.

  MODIF.  app/src/main/java/com/zis/scopa/Decks.kt          (terza tabella di immagini)
          app/src/main/java/com/zis/scopa/Prefs.kt          (DECK_BERG)
          app/src/main/java/com/zis/scopa/SettingsActivity.kt
          app/src/main/res/layout/activity_settings.xml     (terzo pulsante)
          app/src/main/res/values/strings.xml               (nome del mazzo, testo info)
          art/card_back.py                                  (terza palette del dorso)
          README.md
  NUOVO   art/estrai_foglio_bergamasche.py                  (fuori da app/, non entra nell'APK)

Se il repository e' fermo a prima delle ultime consegne, qui dentro ci sono anche il
salvataggio della partita (SavedGame.kt e i tre motori) e il pulsante Esci che chiude
davvero l'app: sono gia' inclusi, non serve fare altro.

--- COSA PROVARE ---

 1. Impostazioni -> Mazzo -> Bergamasche. Le carte devono cambiare subito nei tre giochi.
 2. Guarda l'asso di denari bergamasco: e' un anello vuoto. E' cosi' anche nell'originale
    (e' la carta del marchio del fabbricante, che e' stato tolto). Se ti da' fastidio si
    puo' metterci il marchio ZiS.
 3. Le bergamasche sono piu' strette degli altri due mazzi. E' voluto: e' la loro forma
    vera. Controlla solo che non siano tagliate ai bordi.
 4. Passa da un mazzo all'altro a partita in corso, tornando al menu e rientrando: le
    carte devono cambiare senza che la partita si perda.

--- ATTENZIONE, due cose di sempre ---

 A. Il workflow ha "branches: [main]". Se il tuo ramo principale si chiama master,
    cambia quella riga o non partira' piu' nessuna build.

 B. Devono esserci TUTTE le immagini: 41 ZiS + 41 tradizionali + 41 bergamasche + 7 loghi.
    Ne basta una mancante e la compilazione fallisce.
