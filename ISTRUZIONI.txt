PARTE 1 di 2 - tutto il progetto tranne le immagini  (62 file, sotto il limite di 100 di GitHub)

--- SE HAI GIA' CARICATO LA PARTE 1 ---

Rispetto all'ultima volta cambia solo il README (descrive il mazzo nuovo) e c'e' in piu'
art/estrai_carta_singola.py, che sta fuori da app/ e non entra nell'APK.
Nessuna modifica al codice: puoi saltare la Parte 1 e caricare solo la Parte 2.

--- caricamento ---

NON SERVE CANCELLARE NIENTE: i nomi sono gli stessi che ci sono gia' nel repository,
quindi caricandoli si sovrascrivono.

Vai nella pagina principale del repository, poi Add file -> Upload files, e trascina
dentro il CONTENUTO di questa cartella (non la cartella: quello che c'e' dentro).
Le sottocartelle vengono create da sole.

NON caricare questo file ISTRUZIONI.txt: serve solo a te.

Attenzione a due elementi che iniziano con il punto e che alcuni sistemi nascondono:
  .github/workflows/build-apk.yml
  .gitignore
Su Windows: Esplora file -> Visualizza -> spunta "Elementi nascosti".
Su macOS: Cmd+Shift+punto nel Finder.

--- se il repository e' fermo a prima della revisione del codice ---

  NUOVI   app/src/main/java/com/zis/scopa/Decks.kt
          art/estrai_carta_singola.py   (fuori da app/, non entra nell'APK)
          art/fogli_seme.py             (fuori da app/, non entra nell'APK)

  MODIF.  .github/workflows/build-apk.yml
          README.md
          app/src/main/java/com/zis/scopa/BriscolaActivity.kt
          app/src/main/java/com/zis/scopa/CardView.kt
          app/src/main/java/com/zis/scopa/GameActivity.kt
          app/src/main/java/com/zis/scopa/MainActivity.kt
          app/src/main/java/com/zis/scopa/Prefs.kt
          app/src/main/java/com/zis/scopa/ScopaGame.kt
          app/src/main/java/com/zis/scopa/SettingsActivity.kt
          app/src/main/java/com/zis/scopa/StatsDialog.kt
          app/src/main/java/com/zis/scopa/SystemBars.kt
          app/src/main/java/com/zis/scopa/TresetteActivity.kt
          app/src/main/java/com/zis/scopa/ZisApp.kt
          app/src/main/res/layout/activity_settings.xml
          app/src/main/res/values/strings.xml

--- ATTENZIONE, due cose da controllare ---

 A. Il workflow ha "branches: [main]". Se il tuo ramo principale si chiama master,
    cambia quella riga o non partira' piu' nessuna build.

 B. Le 82 immagini delle carte (card_*, trad_*) devono essere TUTTE nel repository, se no
    la compilazione fallisce. La Parte 2 le contiene tutte.
